import pandas as pd
import numpy as np
import decimal


def gaussian_bayes_classifier(training_data: str, test_data: str, output: str,
                           class_variable: str, is_indexed: bool):
    print('\n Proceed with gaussian bayes classifier')
